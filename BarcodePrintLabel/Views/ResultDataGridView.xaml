<UserControl x:Class="BarcodePrintLabel.Views.ResultDataGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BarcodePrintLabel.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!-- DataGrid -->
        <DataGrid ItemsSource="{Binding Results}" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  Height="300">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Serial Number" Binding="{Binding SerialNumber}" Width="*"/>
                <DataGridTextColumn Header="RS-HM" Binding="{Binding RS_HM}" Width="*"/>
                <DataGridTextColumn Header="RS-VM" Binding="{Binding RS_VM}" Width="*"/>
                <DataGridTextColumn Header="LS-HM" Binding="{Binding LS_HM}" Width="*"/>
                <DataGridTextColumn Header="LS-VM" Binding="{Binding LS_VM}" Width="*"/>
                <DataGridTextColumn Header="Overall" Binding="{Binding Overall}" Width="*"/>
                <DataGridTextColumn Header="Error Code" Binding="{Binding ErrorCode}" Width="*"/>
                <DataGridTextColumn Header="DateTime" Binding="{Binding DateTime}" Width="*"/>
                <DataGridTextColumn Header="QRCode" Binding="{Binding QRCode}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
</UserControl>
